#include<bits/stdc++.h>
#include<vector>
#include<algorithm>
#include<string>
using namespace std;
int main()
{
	vector<string> s1;//original
	cout<<"Enter no of string";
	int n;
	cin>>n;
    s1.push_back("s->S")
    cout<<"Enter terminal in capital letter with production starting with S"
	for(int i=0;i<n;i++)
	{
		 string s2;
		 getline(cin,s2);
		 s1.push_back(s2);
	}
	for (int i = 0; i < n; ++i)
	{
		
		s1.insert(3,".");
	}
	vector<vector<string>>pd;
}
